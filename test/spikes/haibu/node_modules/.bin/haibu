#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../haibu/bin/haibu" "$@"
  ret=$?
else 
  node  "$basedir/../haibu/bin/haibu" "$@"
  ret=$?
fi
exit $ret
