#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../haibu-carapace/bin/carapace" "$@"
  ret=$?
else 
  node  "$basedir/../haibu-carapace/bin/carapace" "$@"
  ret=$?
fi
exit $ret
